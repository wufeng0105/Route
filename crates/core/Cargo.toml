[package]
name = "route-core"
version = "0.1.0"
edition = "2021"
description = "Route 核心逻辑库 — 配置文件解析、安全写入、线路管理"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.8"
dirs = "5"
regex = "1"
